[pytest]
markers =
    integration: Requires live API keys (Claude, Gemini, AstraDB) — slower and costs money
    devnet: Requires SOLANA_DEVNET_PRIVATE_KEY — tests real blockchain submission on Solana devnet
addopts = -v --tb=short
